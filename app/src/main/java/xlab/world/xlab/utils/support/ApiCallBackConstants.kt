package xlab.world.xlab.utils.support

object ApiCallBackConstants {
    const val DELIMITER_CHARACTER = "/"

    const val TOKEN_EXPIRE = "code=401, message=Invalid or expired jwt, Token is expired"
}